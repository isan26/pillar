// Stable identifiers for the providers we support. Used in trace records and
// anywhere a provider needs to name itself.
export const PROVIDER = {
	ANTHROPIC: "anthropic",
	OPENAI: "openai",
} as const
